@model ServicioFormViewModel
@{
    ViewData["Title"] = "Nuevo servicio";
}

<div class="mb-4">
    <h1 class="h3 mb-1">Nuevo servicio</h1>
    <p class="text-elara-muted">Agrega un servicio al catálogo del salón.</p>
</div>

<div class="card card-elara" style="max-width: 760px;">
    <div class="card-body p-4">
        <div asp-validation-summary="ModelOnly" class="text-elara-accent mb-3"></div>
        <form asp-controller="Servicios" asp-action="Create" method="post" enctype="multipart/form-data">
            <partial name="_ServicioForm" model="Model" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-elara">
                    <i class="bi bi-check2 me-1"></i> Guardar servicio
                </button>
                <a asp-controller="Servicios" asp-action="Index" class="btn btn-outline-elara">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
